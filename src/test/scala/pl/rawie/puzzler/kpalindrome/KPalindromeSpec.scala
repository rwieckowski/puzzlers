package pl.rawie.puzzler.kpalindrome

import org.scalatest.matchers.ShouldMatchers
import org.scalatest.FunSpec

class KPalindromeSpec extends FunSpec with ShouldMatchers {
  describe("kpalindrome") {
    it("should") {
      pending
    }
  }
}
